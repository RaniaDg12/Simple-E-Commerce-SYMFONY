{% extends 'base.html.twig' %}

{% block body %}

    <h1>Categories</h1>
        
    {% for category in categories %}
        <a href="{{ path('category_show', {'id': category.id}) }}"  class="btn btn-primary btn-lg">{{ category.title }}</a>
    {% endfor %}

{% endblock %}
